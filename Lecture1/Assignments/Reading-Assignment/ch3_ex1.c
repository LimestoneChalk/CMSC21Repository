#include <stdio.h>

int main (void){
    // initializes variables for use
    int i, j;
    float x, y;

    //assigns said variables
    i = 10;
    j = 20; //integers
    x = 42.2592f;
    y = 5527.0f; //floats

    printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y); //prints the variables
}